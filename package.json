{
  "name": "as-design-system",
  "version": "0.1.0",
  "private": true,
  "description": "Design System basé sur Figma avec génération automatique de composants et tokens",
  "scripts": {
    "dev": "pnpm --filter docs dev",
    "build": "pnpm -r build",
    "type-check": "pnpm -r type-check",
    "clean": "pnpm -r clean"
  },
  "devDependencies": {
    "typescript": "^5.3.3"
  },
  "engines": {
    "node": ">=18.0.0",
    "pnpm": ">=8.0.0"
  },
  "packageManager": "pnpm@8.15.0"
}

